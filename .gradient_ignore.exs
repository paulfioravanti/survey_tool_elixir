[
  # NOTE: Strange error given here that Dialyzer doesn't pick up on, so I'm going to
  # consider it incorrect:
  # `The function call on line 66 is expected to have type decimal() but it has
  # type number()`
  "lib/survey_tool/survey_parser/rating_question.ex:66"
]
